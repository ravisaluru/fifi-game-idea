# Online play needs no configuration: the multiplayer backend (Cloudflare
# Durable Objects) deploys together with the site via `npm run deploy`.
#
# Optional override — only set this when the static site is served from a
# different host than the worker (defaults to same origin):
VITE_API_BASE=
